apiVersion: v1
kind: Service
metadata:
  name: onchain-gateway
  namespace: blockchain-platform
spec:
  selector:
    app: onchain-gateway
  ports:
    - name: grpc
      port: 443
      targetPort: grpc
    - name: ws
      port: 80
      targetPort: ws

